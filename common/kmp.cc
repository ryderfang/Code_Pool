#include <iostream>
#include <string>
using namespace std;

[arr, &next]() {
    next[0] = next[1] = 1;
    for (size_t i = 2; i < arr
}

void simple_kmp(const string& ori, const string& pat) {
     
}

int main() {
    string text = "this is the origin text";
    string pattern = "the";
    return 0;
}

