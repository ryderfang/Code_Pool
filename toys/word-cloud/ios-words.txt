#! 某厂客户端开发 258 篇晋升 PPT 关键词

#T9 (108)

安卓无权限桌面弹框，拉活，触达


#T10 (88)

解耦，LLVM，Clang LibTooling
上传组件，超时重传，分片，解耦
组件化，耦合，滥用反射
响应式，统一数据，布局，弹幕，双线程
Flutter，Hook
OpenGL，GLDrawable
音视频，颜色，实时图像处理
富文本渲染，TextKit，YYKit
热加载，TCP/USB，Swift
Hippy，NativeVue，调试，usbmuxd
图片加载，OpenGL，表情包，Flutter
SuperPlayer，预加载，耗时优化
热加载，Injectioniii，Clang，LLVM，Bitcode
音视，动画，PAG
硬编码，全链路优化
SDK，业务定制，组件化，标准化，动态化，解耦，路由，数据驱动
渲染，纹理复用，PAG
离线包，更新
插件预加载，Shadow，音频
渲染链，滤镜，数据驱动，性能优化
MxFlutter
播放，渲染，预加载，组件
动态化，Hippy，调试
组件化，独立编译，复用，接口，测试
视频通话，SVC，时域，编码
CrossEngine，UE，FBO
Hippy，动态绑定
下载优化，CDN，QUIC
逆向，黑产
数据埋点，上报，大同SDK
Flutter，热更新，亚秒级，异常监控
跨平台，Electron，C++ addon
多语言，输入法
小游戏，Unity，沙盒，安全性
编译耗时，clang，dyld，pod
Crash，堆栈分析
首帧速度，直播，CDN
加载优化，绘制，布局，资源，线程，秒开，推荐重排
CrossEngine，Metal
直播，P2P，HLS
CrossEngine，资源加载，换装，动画复用
文字排版，Layout，全链接，分层优化
渲染链，GPUImage，LightSDK
视频通话，弱网，分辩率，性能，音画同步
内存监控，APM，拦截请求
消息处理，生产者消费者，数据库，拉取时机
Bugly，ANR，组件化，解耦
Doc，文档，分段加载，性能
APM，堆栈，性能，卡顿
SDK，黑产，反调试
组件化，耦合，服务依赖注入，插件化，路由
Flutter，手势，TDF
UE，引擎，framework，离屏录制
优化，耗时，TEXT 段，图片加载
显存分配，检测，GAPII Hook
音视频通话，表情驱动
研发模式，DevOps
编译优化，沙盒
录屏，GPU，APNG，解码
LightSDK，美颜
Hippy，DSL，可视化埋点
音频内录，视频播放
终端 Replay，Flutter
3D，人脸，特效
材质，Shader，资源
Xcode，Kotlin Native，KMM
播放，字幕，解耦，轨道分离
模块依赖，解耦，IWYU，重复符号
MxFlutter，Dart，JS
LightSDK，脚本，绑定
组件化编译，插件化
MxFlutter，JSRuntime，符号源码调试切换
性能测试，监控
实时，音视频，丢包，抖动，时延
音视频，内存优化，画中画
资源发布，SDK，更新，差量
App Bundle，组件动态化，数据治理
虚拟应用，Hook，多进程
算子，TNN，模型量化，人脸检测
支付链路优化，安全，加密
内存监控，卡顿监控，音视频
逆向，_TEXT 段减包，音频拟合

#T11 (41)

Flutter，DartNative，Block
音视频，编码，复用
渲染引擎，优化，OpenGL，Metal
QUIC，播放器，SuperPlayer，Andromeda，Cronet，TQuic
埋点，VideoReporter
动态化，NativeVue，首屏耗时
跨平台，网络，线程，XPlatform，MSF
小程序，二维码，Flutter，Dart
文档，渲染，排版，数据
数据模型，架构一体，打包
X5，浏览器内核，渲染，管线
AOW，模拟器
播放器，中台，码率，清晰度
渲染，贴纸，fusion，LightSDK
STT，语音转文本，KWS，语音唤醒
大屏播放器，清晰度，码率，自动化测试，日志
模块化，解耦，路由，自动化
PC，RichEdit，布局，绘制，事件，数据驱动，排版
无障碍化，加载优化，耗时，PSNR，白屏
符号表，私有函数，模块，基址
解耦，自动化，安全
隐私合规，字节码插桩，动态下发
编译，优化，远程编译
文件传输，跨平台，解耦
动态化，Flutter，TDF，渲染引擎，Pipeline
单测，性能，框架，MVVM，MVI
直播，中台，视频，播放
动态化，Flutter，DNFlutter，AST
架构，高内聚低耦合，编译
架构，组件化，分层设计，单向依赖，路由，壳工程
防劣化，质量评估，编译，Bazel，二进制化
LocalPod，XCRemoteCache，CDN 化
中台播放器，研效
线程模型，序列，无锁，跨平台，内核
数据治理，曝光，点击
动态化，DSL，响应式，渲染，埋点
热修复，Tinker，QFix
内核优化，首屏时间
Pag，Skia，OpenGL
Flutter，卡顿，监控
浏览器内核，内存优化，OOM

#T12 (19)

工业化，解耦，研效，Bazel
动态化，TDF，Hippy
红包，CDN，高并发，研效andromeda
动态化
启动，性能，DevOps，主干开发
埋点，AOP，响应式 UI
Shadow 插件化
实时音视频，低延迟，监控体系
浏览器内核，Blink
小程序，快，大，生态
SSR，耗时，优化，CSR
函数式编程，响应式组件化，单测
DOM，渲染树，布局，事件，动画，数据协议
文档，架构，OOXML，Bazel
TDF Core，Dart，MxFlutter，C++，命令式
AR，Filament，Bullet，PhysX，引擎
Hippy，TDF，RaftKit
超高清视频，ThumbPlayer，视频解码，丢帧

#T13 (2)

IL 虚拟机，Flutter，动态化，工业化，组件化，插件化，解耦
ThumbPlayer，统一框架，组件化，格式解析，数据解码，后处理与渲染

x次5星，x次4星，x次3星